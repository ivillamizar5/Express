{
  "name": "express",
  "version": "1.0.0",
  "main": "agendaTelefonica.js",
  "scripts": {
    "build:ui": "rm -rf dist && cd ../iniciando/empezarDeNuevo/Empezando && npm run build && cp -r dist",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node iniciando.js",
    "dev": "nodemon iniciando.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  },
  "description": ""
}
